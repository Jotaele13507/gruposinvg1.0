CREATE TABLE logogi (
    id_logo INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    id_usuariol INT NOT NULL,
    dir_logo VARCHAR(100) NOT NULL,
    
    FOREIGN KEY fk_id_usuariol(id_usuariol) REFERENCES usuarios(id)
);